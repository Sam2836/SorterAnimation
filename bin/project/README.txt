1. arrayRandomizer.java and SortingProcess.java need to be compiled in the same package as SortingVisual.java
2. Everything breaks when run from a command line, so the actual "run" button is needed
3. Once everything is in the same place, SortingVisual.java can be run